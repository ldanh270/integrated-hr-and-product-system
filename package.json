{
  "scripts": {
    "dev": "concurrently \"bun run dev:backend\" \"bun run dev:frontend\"",
    "dev:all": "concurrently \"bun run dev:backend\" \"bun run dev:frontend\" \"bun run db:studio\"",
    "install:all": "concurrently \"bun run install:backend\" \"bun run install:frontend\"",
    "install:backend": "cd backend && bun install",
    "install:frontend": "cd frontend && bun install",
    "dev:backend": "cd backend && bun --watch src/index.ts",
    "dev:frontend": "cd frontend && bun run dev",
    "build": "bun run build:frontend",
    "build:frontend": "cd frontend && bun run build",
    "db:migrate": "cd backend && bunx prisma migrate dev",
    "db:generate": "cd backend && bunx prisma generate",
    "db:studio": "cd backend && bunx prisma studio",
    "hash-password": "cd backend && bun run src/scripts/hash-password.ts"
  },
  "dependencies": {
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "prettier-plugin-tailwindcss": "^0.8.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "concurrently": "^9.2.1"
  }
}
